import React from 'react';
import * as Styled from './styles';

const Header = () => {
	return (
		<Styled.Container>
			<Styled.Wrapper>Header</Styled.Wrapper>
		</Styled.Container>
	);
};

export default Header;
